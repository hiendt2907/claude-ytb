# Core
anthropic>=0.40.0          # LLM: ý tưởng + kịch bản (Claude)
pydantic>=2.0             # validation tại ranh giới hệ thống
pydantic-settings>=2.0    # config qua env vars
python-dotenv>=1.0

# Voiceover (TTS)
elevenlabs>=1.0           # TTS chất lượng cao (tuỳ chọn)
edge-tts>=6.1             # TTS miễn phí (Microsoft Edge)

# Media + render
moviepy>=1.0.3            # dựng video
Pillow>=10.0             # xử lý ảnh / thumbnail
# ffmpeg phải được cài qua hệ thống: `brew install ffmpeg`

# Upload + analytics
google-api-python-client>=2.0
google-auth-oauthlib>=1.0

# Dev
pytest>=8.0
pytest-asyncio>=0.23
pytest-cov>=5.0
