# Assembler core (scanning/assignment/naming) không cần dependency ngoài stdlib.
# Render (duration.py, render.py) gọi ffmpeg/ffprobe qua subprocess — cài qua hệ thống:
#   brew install ffmpeg

# Web UI (localhost)
fastapi>=0.110
uvicorn>=0.29
jinja2>=3.1
python-multipart>=0.0.9

# Content pipeline (script → voice → pexels → publish) — xem CLAUDE.md 2026-07-09
edge-tts>=6.1
google-auth-oauthlib>=1.2
google-api-python-client>=2.120

# Dev / test
pytest>=8.0
pytest-cov>=5.0
httpx>=0.27
