"""Content pipeline — sinh kịch bản (Claude), voiceover (edge-tts), tải Pexels theo cảnh.

Nối vào trước `assembler/` (render-only, giữ nguyên) để tạo luồng xuyên suốt:
script → voice → pexels (đổ vào scene folders) → render/preview có sẵn → publish.
"""
