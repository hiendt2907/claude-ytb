Kịch bản JSON sau đây KHÔNG đạt cổng chất lượng, cần sửa lại — GIỮ NGUYÊN chủ
đề/câu chuyện, chỉ sửa đúng các lỗi liệt kê bên dưới:

KỊCH BẢN HIỆN TẠI:
{script_json}

LỖI CẦN SỬA:
{violations}

CHỈ trả về JSON đã sửa, đúng schema cũ (title/description/segments[{{narration,
visual_keywords}}]), không kèm text giải thích, không bọc trong ```.
